apiVersion: apps/v1
kind: Deployment
metadata:
  name: pi-agent-controller-processor
  namespace: pi-agent
  labels:
    app: pi-agent-controller-processor
spec:
  replicas: 1
  selector:
    matchLabels:
      app: pi-agent-controller-processor
  template:
    metadata:
      labels:
        app: pi-agent-controller-processor
    spec:
      containers:
      - name: pi-agent-controller-processor
        image: pi-agent-controller-processor:latest
        ports:
        - containerPort: 50052
        resources:
          limits:
            memory: "256Mi"
            cpu: "500m"
          requests:
            memory: "128Mi"
            cpu: "250m"
        env:
        - name: STAGE
          value: "prod"
---
apiVersion: v1
kind: Service
metadata:
  name: pi-agent-controller-processor-service
  namespace: pi-agent
spec:
  type: LoadBalancer
  selector:
    app: pi-agent-controller-processor
  ports:
  - protocol: TCP
    port: 50052
    targetPort: 50052
